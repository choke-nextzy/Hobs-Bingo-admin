export interface User {
    username: string;
    password: string;
}

export interface IUserProfile {
    userId: string;
    displayName: string;
    statusMessage: string;
    pictureUrl: string;
    idToken: string;
  }
